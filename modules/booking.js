// ===============================
// BOOKING MODULE (placeholder)
// ===============================

export function openBookingForm(cruise) {
  alert(
    `Booking request started for:\n\n` +
    `Cruise Line: ${cruise.line}\n` +
    `Ship: ${cruise.ship}\n` +
    `Itinerary: ${cruise.itinerary}\n\n` +
    `Full booking flow coming soon!`
  );
}
